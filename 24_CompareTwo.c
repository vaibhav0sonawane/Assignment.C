#include<stdio.h>
int main(){
int a,b;
printf("enter two numbers:");
scanf("%d""%d",&a,&b);
if((a<b)||(a>b))
{
 printf("%d is greater then %d",a,b);
}
else
     printf("both numbers are equal");
return 0;
}
